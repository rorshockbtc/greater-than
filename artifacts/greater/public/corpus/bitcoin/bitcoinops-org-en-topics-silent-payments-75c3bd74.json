{
  "slug": "bitcoinops-org-en-topics-silent-payments-75c3bd74",
  "source_url": "https://bitcoinops.org/en/topics/silent-payments/",
  "source_label": "Optech — Silent payments topic",
  "excerpt": "Silent payments produce taproot outputs that are indistinguishable on-chain from any other single-sig taproot spend. There is no notification step and no per-payment interactivity required from the recipient. The cost is on the recipient side: continuously scanning new blocks to detect payments addressed to them.",
  "body": "Source excerpt:\nSilent payments produce taproot outputs that are indistinguishable on-chain from any other single-sig taproot spend. There is no notification step and no per-payment interactivity required from the recipient. The cost is on the recipient side: continuously scanning new blocks to detect payments addressed to them.\n\n---\n\nCited by leaf \"Silent payments (BIP352)\":\nSilent payments give a recipient one static identifier — looks like an address, starts with `sp1` — that they can put in a bio or on a donation page, where every payment to it produces a unique, fresh on-chain output that only the recipient can spend [1]. There is no on-chain notification, no extra round-trip with the sender, and no shared secret stored anywhere reusable.\n\nThe construction is elliptic-curve Diffie-Hellman between the sender's input keys and the recipient's published key, mixed with the transaction's outpoints to produce a derivation tweak. The recipient scans new blocks looking for outputs whose key matches what their own scan key would derive against the sender's input pubkeys. From the chain's perspective the output looks like any other taproot output with no metadata of any kind [2].\n\nThe design fixes both halves of the static-identifier problem at once: the donor gets the convenience of a fixed thing they can copy from a webpage, and the recipient gets per-payment uncorrelatable outputs without ever publishing them. The honest tradeoff is scanning cost. The recipient (or their full node) has to do an EC operation per transaction in the chain to detect incoming payments, which is fine for a personal node and harder for a phone wallet — current implementations rely on dedicated indexers or BIP158-style filters tuned for silent payments [3]. BIP352 is finalized; the rollout is happening one wallet at a time.",
  "cited_by": [
    {
      "pack": "bitcoin",
      "leaf_id": "silent-payments",
      "leaf_label": "Silent payments (BIP352)",
      "leaf_brief": "Silent payments give a recipient one static identifier — looks like an address, starts with `sp1` — that they can put in a bio or on a donation page, where every payment to it produces a unique, fresh on-chain output that only the recipient can spend [1]. There is no on-chain notification, no extra round-trip with the sender, and no shared secret stored anywhere reusable.\n\nThe construction is elliptic-curve Diffie-Hellman between the sender's input keys and the recipient's published key, mixed with the transaction's outpoints to produce a derivation tweak. The recipient scans new blocks looking for outputs whose key matches what their own scan key would derive against the sender's input pubkeys. From the chain's perspective the output looks like any other taproot output with no metadata of any kind [2].\n\nThe design fixes both halves of the static-identifier problem at once: the donor gets the convenience of a fixed thing they can copy from a webpage, and the recipient gets per-payment uncorrelatable outputs without ever publishing them. The honest tradeoff is scanning cost. The recipient (or their full node) has to do an EC operation per transaction in the chain to detect incoming payments, which is fine for a personal node and harder for a phone wallet — current implementations rely on dedicated indexers or BIP158-style filters tuned for silent payments [3]. BIP352 is finalized; the rollout is happening one wallet at a time."
    }
  ],
  "generated_by": "build-catalog-corpus",
  "generated_at": "static"
}
