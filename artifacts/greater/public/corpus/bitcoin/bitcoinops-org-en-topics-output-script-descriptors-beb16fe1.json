{
  "slug": "bitcoinops-org-en-topics-output-script-descriptors-beb16fe1",
  "source_url": "https://bitcoinops.org/en/topics/output-script-descriptors/",
  "source_label": "Bitcoin Optech — Privacy topic",
  "excerpt": "The common-input-ownership heuristic assumes that all inputs to a transaction are controlled by the same entity. It is the workhorse assumption of most surveillance heuristics, and it is broken intentionally by coinjoin and payjoin constructions.",
  "body": "Source excerpt:\nThe common-input-ownership heuristic assumes that all inputs to a transaction are controlled by the same entity. It is the workhorse assumption of most surveillance heuristics, and it is broken intentionally by coinjoin and payjoin constructions.\n\n---\n\nCited by leaf \"Address reuse and the default privacy floor\":\nIf two payments arrive at the same Bitcoin address, anyone watching the chain can permanently link them — same recipient, same balance, same spending history once those coins move. There is no cryptographic step required; it's just public data joined on a string match. This is the single biggest privacy leak in everyday Bitcoin use, and it's the one the protocol gives you the most help avoiding [1].\n\nHD wallets (BIP32) were designed around this. From a single seed, a wallet derives a fresh receiving address for every incoming payment along a deterministic path, so the user never has to manage keys by hand or copy old addresses [2]. Modern wallets default to this — you tap \"receive\" and get a new address each time. The leak shows up when people paste a static address into a bio, a donation page, or a shop checkout, or when an exchange withdrawal gets reused as an inbound for the next deposit.\n\nThe other half of the same problem is the common-input-ownership heuristic: when a transaction spends multiple inputs, the surveillance assumption is that one wallet controls all of them [3]. Combine that with one reused address and a chain analyst can usually walk a substantial portion of someone's UTXO set in a few hops. Coinjoin, payjoin, and silent payments all exist to break some piece of this default linkage; the cheapest defense, however, is just not reusing addresses in the first place.",
  "cited_by": [
    {
      "pack": "bitcoin",
      "leaf_id": "address-reuse",
      "leaf_label": "Address reuse and the default privacy floor",
      "leaf_brief": "If two payments arrive at the same Bitcoin address, anyone watching the chain can permanently link them — same recipient, same balance, same spending history once those coins move. There is no cryptographic step required; it's just public data joined on a string match. This is the single biggest privacy leak in everyday Bitcoin use, and it's the one the protocol gives you the most help avoiding [1].\n\nHD wallets (BIP32) were designed around this. From a single seed, a wallet derives a fresh receiving address for every incoming payment along a deterministic path, so the user never has to manage keys by hand or copy old addresses [2]. Modern wallets default to this — you tap \"receive\" and get a new address each time. The leak shows up when people paste a static address into a bio, a donation page, or a shop checkout, or when an exchange withdrawal gets reused as an inbound for the next deposit.\n\nThe other half of the same problem is the common-input-ownership heuristic: when a transaction spends multiple inputs, the surveillance assumption is that one wallet controls all of them [3]. Combine that with one reused address and a chain analyst can usually walk a substantial portion of someone's UTXO set in a few hops. Coinjoin, payjoin, and silent payments all exist to break some piece of this default linkage; the cheapest defense, however, is just not reusing addresses in the first place."
    }
  ],
  "generated_by": "build-catalog-corpus",
  "generated_at": "static"
}
